GeoField — P4.7 Dark Mode + RTL/LTR + Motion System

Project: GeoField
Track: Frontend / Product / UX / UI
Phase: P4 — GeoField Design System
Gate: P4.7
Status: COMPLETED / PASS
Repository: "VahidAfshin89/geofield-frontend"

---

1. Gate Objective

P4.7 با هدف نهایی‌سازی سه حوزه مهم و Cross-Platform در Design System GeoField انجام شد:

Dark Mode
+
RTL / LTR
+
Motion

این Gate باید مشخص کند که Design System GeoField چگونه در:

Light
Dark
RTL
LTR
Motion Enabled
Reduced Motion

رفتار می‌کند.

P4.7 بر مبنای Gateهای قبلی ساخته شده است:

P4.2
Color + Semantic Tokens

P4.3
Typography + Spacing + Layout

P4.4
Core Component Architecture

P4.5
Navigation + Map Controls

P4.6
Operational State + Feedback + Recovery

---

2. P4 Position

P4.1
Design System Foundation
        ↓
P4.2
Color + Semantic Tokens
        ↓
P4.3
Typography + Spacing + Layout
        ↓
P4.4
Core Component Architecture
        ↓
P4.5
Navigation + Map Controls
        ↓
P4.6
Operational State + Feedback + Recovery
        ↓
P4.7
Dark Mode + RTL/LTR + Motion
        ↓
P4 Consolidation / Design Gate

---

3. Gate Boundary

P4.7:

Theme Behavior
+
Direction Behavior
+
Motion Behavior

را تعریف می‌کند.

P4.7:

Flutter Production Implementation = NO
React Production Implementation = NO
Backend Change = NO
Database Change = NO
Migration = NO

است.

---

4. Core Principle

Theme، Direction و Motion باید:

Predictable
Semantic
Accessible
Cross-platform
Localization-ready
State-aware

باشند.

اصل بنیادی:

Same Meaning
+
Adaptive Presentation

---

5. Dark Mode Philosophy

Dark Mode نسخه Inverted Light Mode نیست.

هر Theme باید Semantic Roleهای خودش را داشته باشد.

مدل:

Semantic Token
        ↓
Light Value
Dark Value

---

6. Theme Architecture

Architecture:

Primitive
    ↓
Semantic Token
    ↓
Theme Mapping
    ↓
Component Alias
    ↓
Component

مثال:

interactive.primary
        ↓
Light  = #2563EB
Dark   = #60A5FA

Component نباید مستقیماً Theme Color را Hard-code کند.

---

7. Theme Modes

Baseline:

Light
Dark

Supported across:

Web
Mobile

---

8. Theme Selection

Theme selection may support:

System
Light
Dark

در Implementation.

Design System باید هر سه رفتار را از نظر Semantic پشتیبانی کند.

---

9. Theme Persistence

در آینده، Preference کاربر می‌تواند Persist شود.

مثلاً:

Theme = Dark

پس از Restart نیز قابل بازیابی است.

جزئیات Storage مربوط به Technical Foundation خواهد بود.

---

10. Theme Switching

Theme Switching باید:

Predictable
Fast
Non-destructive
Context-preserving

باشد.

تغییر Theme نباید باعث:

Navigation Reset
Data Loss
Draft Loss
Map Context Loss

شود.

---

11. Theme and State

State semantics در Light و Dark ثابت می‌ماند.

برای مثال:

sync.failed

در هر دو Theme به یک معناست.

فقط Representation تغییر می‌کند.

---

12. Semantic Stability

این Stateها باید Semantic ثابت داشته باشند:

Success
Warning
Error
Info
Pending
Verified
Failed
Offline
Sync
GNSS
Attention

Theme نباید معنای آنها را تغییر دهد.

---

13. Light Theme

Light Theme از Baseline P4.2 استفاده می‌کند.

نمونه:

surface.canvas
    → #F8FAFC

surface.default
    → #FFFFFF

content.primary
    → #0F172A

interactive.primary
    → #2563EB

---

14. Dark Theme

Dark Theme از Baseline P4.2 استفاده می‌کند.

نمونه:

surface.canvas
    → #020617

surface.workspace
    → #0F172A

surface.default
    → #1E293B

content.primary
    → #F8FAFC

interactive.primary
    → #60A5FA

---

15. Theme Surface Hierarchy

Light و Dark باید Surface hierarchy مشابهی داشته باشند.

Canvas
Workspace
Default
Raised
Floating
Overlay
Inverse

این باعث می‌شود Component hierarchy در هر دو Theme حفظ شود.

---

16. Theme Content Hierarchy

Content roles:

Primary
Secondary
Tertiary
Muted
Disabled
Inverse

باید در هر Theme از نظر Hierarchy قابل تشخیص باشند.

---

17. Theme Border Hierarchy

Borders:

Subtle
Default
Strong
Focus
Selected
Error

در هر Theme باید قابل تشخیص باشند.

---

18. Dark Mode Operational States

Dark Mode باید Stateهای P4.6 را حفظ کند:

Saved Locally
Waiting for Sync
Uploading
Synced
Verified
Failed
Needs Attention
Offline
GNSS

---

19. Dark Mode Accessibility

Dark Mode نباید صرفاً با کاهش Brightness ساخته شود.

باید:

Contrast
Hierarchy
Focus
State Visibility

را حفظ کند.

---

20. Theme and GIS

Dark UI ممکن است همزمان با:

Satellite
Dark Basemap
Light Basemap
Vector Layers
Raster Layers

نمایش داده شود.

بنابراین UI Overlay باید در هر Theme قابل مشاهده بماند.

---

21. Dark Map Context

Dark UI الزاماً به معنی Dark Map نیست.

این دو مستقل هستند:

UI Theme
≠
Map Basemap

مثال:

Dark UI
+
Satellite Basemap

می‌تواند کاملاً معتبر باشد.

---

22. Basemap Independence

Map Basemap می‌تواند:

Light
Dark
Satellite
Terrain

باشد، مستقل از Theme UI.

---

23. Theme and Map Controls

Map Controls باید از Semantic Tokens استفاده کنند.

مثلاً:

Control Surface
→ surface.floating

Focus
→ focus.default

Selected
→ interactive.selected

---

24. Theme Transition

Theme Transition در صورت استفاده باید کوتاه و غیرمزاحم باشد.

Avoid:

Long Theme Animation

Preferred:

Short Cross-fade

یا Transition ساده.

---

25. Theme and Motion

Theme switching نباید در طول Motionهای مهم باعث:

Jank
State Loss
Unexpected Reset

شود.

---

26. RTL / LTR Architecture

RTL/LTR یک Layout concern است.

Architecture:

Content Language
    ↓
Direction
    ↓
Layout
    ↓
Component

---

27. Supported Directions

RTL
LTR

هر دو باید First-class باشند.

---

28. RTL Baseline

Persian UI:

Direction = RTL

اما Product Architecture همان باقی می‌ماند.

---

29. LTR Baseline

English UI:

Direction = LTR

و همان:

Navigation
Workspace
Component
State

استفاده می‌شود.

---

30. Direction Independence

RTL/LTR نباید:

Separate Product
Separate Component API
Separate Domain Logic

بسازد.

---

31. Logical Layout

Implementation باید در صورت امکان از Logical Properties استفاده کند:

padding-inline
margin-inline
inset-inline

به‌جای وابستگی شدید به:

left
right

---

32. Text Alignment

Text Alignment باید Semantic باشد.

در RTL:

Primary reading flow
→ Right

در LTR:

Primary reading flow
→ Left

اما بعضی technical values ممکن است Alignment اختصاصی خود را داشته باشند.

---

33. Numeric Content

اعداد، مختصات و مقادیر فنی باید در RTL و LTR پایدار بمانند.

Examples:

35.6892
51.3890
12.4 ha
78%
ID-48271

نباید به دلیل Direction باعث شکست UI شوند.

---

34. Mixed-direction Content

محتوای ترکیبی:

Persian + English
Persian + Numbers
Persian + Coordinates
Persian + IDs

باید قابل خواندن باقی بماند.

این موضوع مخصوصاً در GIS و فرم‌های عملیاتی مهم است.

---

35. Directional Icons

Icons که مفهوم Direction دارند ممکن است Mirror شوند:

Back
Forward
Next
Previous
Chevron
Arrow

---

36. Non-directional Icons

این Icons نباید بدون دلیل Mirror شوند:

Location
Camera
Fire
Cloud
Warning
Check

---

37. Navigation in RTL

Navigation order باید با Direction سازگار باشد، ولی Semantic Order حفظ شود.

مثلاً:

Home
Map
My Work
Drafts
More

همان مدل باقی می‌ماند.

---

38. Drawer Direction

در RTL:

Primary Navigation Drawer
→ Right-side origin where appropriate

در LTR:

Primary Navigation Drawer
→ Left-side origin where appropriate

Implementation نهایی ممکن است بر اساس Platform تغییر کند.

---

39. Bottom Navigation

Bottom Navigation باید در هر دو Direction:

RTL
LTR

قابل استفاده باشد.

Selection indicator باید واضح باشد.

---

40. Map Direction

Map itself is spatial.

بنابراین:

RTL
≠
Mirror Map

به‌صورت پیش‌فرض.

Map content باید از Spatial Reference و Map Engine semantics خودش پیروی کند.

---

41. Map Controls in RTL

Map Controls باید جایگاه مناسب داشته باشند بدون اینکه صرفاً همه چیز را Mirror کنند.

مثلاً:

Zoom
Location
Layers

ممکن است از نظر Position در UI جابه‌جا شوند، اما Map Navigation semantics باید حفظ شود.

---

42. Forms in RTL

Form:

Label
Input
Helper
Error

باید Reading Flow مناسب RTL داشته باشد.

اما:

Phone
Coordinates
Numeric
Technical ID

می‌توانند Presentation متفاوت داشته باشند.

---

43. Tables in RTL

Web tables باید:

Header
Rows
Actions
Values

را با Direction مناسب نمایش دهند.

اما Numeric Columns ممکن است Alignment خاص خود را داشته باشند.

---

44. Dialog in RTL

Dialog Actions باید semantic hierarchy خود را حفظ کنند:

Primary
Secondary
Destructive
Cancel

صرفاً جابه‌جایی تصویری نباید meaning را خراب کند.

---

45. Motion System

Motion بخشی از UX Language است، نه Decoration.

هدف:

Understand
Navigate
Confirm
Transition
Recover

---

46. Motion Principles

Motion باید:

Purposeful
Short
Predictable
Non-blocking
Context-preserving
Accessible

باشد.

---

47. Motion Categories

Baseline:

Micro Interaction
State Transition
Navigation Transition
Feedback Motion
Loading Motion
Spatial Motion

---

48. Micro Interaction

نمونه:

Button Press
Toggle
Chip Selection
Checkbox
Input Focus

Motion کوتاه و کم‌دامنه.

---

49. State Transition Motion

برای:

Loading
Success
Error
Sync
Verification

ممکن است Motion استفاده شود.

---

50. Navigation Motion

Transitions:

Page
Panel
Drawer
Bottom Sheet

باید جهت‌دار و قابل پیش‌بینی باشند.

---

51. Map Motion

Map may animate:

Camera movement
Selection focus
Location centering
Feature focus

اما Animation نباید موقعیت دقیق کاربر را دشوار کند.

---

52. Sync Motion

Sync می‌تواند Motion داشته باشد:

Pending
 ↓
Uploading
 ↓
Success

یا:

Uploading
 ↓
Failed

Motion باید با State واقعی هماهنگ باشد.

---

53. Upload Motion

Upload/Sync Progress می‌تواند از:

Progress Indicator

استفاده کند.

Fake Progress ممنوع است.

---

54. Loading Motion

Loading:

Spinner
Skeleton
Progress

بر اساس Context انتخاب می‌شود.

---

55. Success Motion

Success motion باید:

Brief
Clear
Non-blocking

باشد.

مثلاً:

Checkmark
Subtle transition

اما Success متن/State را جایگزین نمی‌کند.

---

56. Error Motion

Error motion نباید شدید یا اضطراب‌آور باشد.

مثلاً:

Subtle shake

تنها در موارد محدود.

Text و Recovery Action اصلی باقی می‌مانند.

---

57. Bottom Sheet Motion

Bottom Sheet:

Collapsed
→
Expanded

باید smooth باشد ولی بیش از حد طولانی نباشد.

---

58. Drawer Motion

Navigation Drawer باید:

Open
Close

Transition قابل پیش‌بینی داشته باشد.

---

59. Motion Duration Baseline

Baseline:

Token| Value
"motion.instant"| 0ms
"motion.micro"| 100ms
"motion.fast"| 160ms
"motion.normal"| 240ms
"motion.slow"| 360ms
"motion.emphasis"| 480ms

این مقادیر Baseline هستند و در Implementation با Platform Constraints اعتبارسنجی می‌شوند.

---

60. Motion Hierarchy

Micro
→ fastest

Component State
→ fast

Navigation
→ normal

Major Transition
→ slow

Attention / Emphasis
→ limited use

---

61. Easing

Motion باید از Easingهای محدود و Consistent استفاده کند:

Ease-out
Ease-in
Ease-in-out
Linear

"Linear" عمدتاً برای:

Progress
Continuous Rotation

مناسب است.

---

62. Motion and Context Preservation

Animation نباید باعث شود کاربر موقعیت خود را در Product گم کند.

مثلاً:

Map
→ Inspector

باید رابطه فضایی قابل فهمی داشته باشد.

---

63. Motion and State Truth

Motion فقط باید State واقعی را نشان دهد.

مثلاً:

Uploading Animation

نباید وقتی Upload واقعاً متوقف شده است ادامه پیدا کند.

---

64. Reduced Motion

کاربر ممکن است Motion را کاهش دهد.

Architecture باید:

Normal Motion
Reduced Motion

را پشتیبانی کند.

---

65. Reduced Motion Rule

در Reduced Motion:

Animation
→ Shortened / Simplified / Removed

ولی:

State
Feedback
Hierarchy

حفظ می‌شود.

---

66. What Must Never Depend on Motion

این اطلاعات نباید فقط با Motion منتقل شوند:

Error
Success
Sync Status
Verification
Offline
GNSS
Critical Warning

---

67. Reduced Motion and GNSS

GNSS:

Searching
Available
Uncertain
Unavailable

باید بدون Animation نیز قابل فهم باشد.

---

68. Reduced Motion and Sync

Sync باید بدون Animation نیز قابل درک باشد:

Uploading
Synced
Failed

Animation تنها Enhancer است.

---

69. Reduced Motion and Navigation

Navigation transitions می‌توانند:

Fade
Minimal Transition

شوند.

Context باید حفظ شود.

---

70. Motion and Accessibility

Motion نباید:

Cause Confusion
Block Interaction
Hide Focus

کند.

---

71. Motion and Touch

در Mobile، Motion نباید باعث شود User Interaction بیش از حد طول بکشد.

به‌خصوص:

Save
Retry
Capture
Confirm

نباید با Animation طولانی مسدود شوند.

---

72. Motion and Outdoor Field Use

Field Users ممکن است:

Moving
Bright Sunlight
One-handed
Under Cognitive Load

باشند.

پس Motion باید:

Subtle
Fast
Informative

باشد.

---

73. Motion Anti-patterns

Avoid:

Long Intro Animations
Decorative Motion
Continuous Background Animation
Fake Progress
Excessive Parallax
Excessive Bounce
Motion for Every Click

---

74. Dark Mode + Motion

Motion باید در هر Theme:

Consistent

باشد.

تغییر Theme نباید Motion semantics را تغییر دهد.

---

75. RTL/LTR + Motion

Directional transitions باید با Direction هماهنگ شوند.

مثلاً:

RTL Drawer
→ Right-origin

و:

LTR Drawer
→ Left-origin

اما Motion برای Map Spatial Position نباید کورکورانه Mirror شود.

---

76. Component Motion Contract

Core Components در P4.4 می‌توانند Motion داشته باشند:

Button
Input
Chip
Dialog
Bottom Sheet
Navigation
Status
Loading

هر Component باید Motion را در حد نیاز استفاده کند.

---

77. Motion Token Architecture

Architecture:

Motion Token
    ↓
Component Transition
    ↓
Workspace Transition

مثال:

motion.fast
    ↓
Button Press

یا:

motion.normal
    ↓
Bottom Sheet

---

78. Theme Token Architecture

Architecture:

Semantic Color
      ↓
Light / Dark
      ↓
Component Alias

---

79. Direction Architecture

Architecture:

Locale
 ↓
Direction
 ↓
Logical Layout
 ↓
Component Rendering

---

80. Three-system Relationship

P4.7 سیستم‌های زیر را به یکدیگر متصل می‌کند:

Theme
+
Direction
+
Motion

اما این‌ها باید مستقل از Domain Logic باقی بمانند.

---

81. Cross-platform Rule

Web و Mobile باید:

Same Semantics
Same Tokens
Same State Meaning

داشته باشند.

ولی:

Different Rendering

مجاز است.

---

82. Web Theme Behavior

Web باید:

Light
Dark
System

را در آینده پشتیبانی کند.

---

83. Mobile Theme Behavior

Mobile:

Light-first
Dark Supported

است.

Theme باید برای محیط میدانی قابل خواندن باشد.

---

84. Web RTL/LTR

Web باید Full RTL/LTR Support داشته باشد:

Navigation
Forms
Tables
Dialogs
Map Panels
Inspector
Filters

---

85. Mobile RTL/LTR

Mobile باید:

Forms
Navigation
Bottom Sheets
Dialogs
Actions

را در RTL/LTR پشتیبانی کند.

---

86. Cross-platform State Consistency

مثلاً:

sync.failed

در Web و Mobile باید یک معنی داشته باشد.

ممکن است Web:

Banner

و Mobile:

Bottom Sheet

نمایش دهد.

---

87. Dark Mode and Component Tokens

Components نباید:

if dark then #xxxxxx

داشته باشند.

به‌جای آن:

Semantic Token

مصرف شود.

---

88. Direction and Component APIs

Component API نباید برای RTL/LTR دو نسخه جدا داشته باشد.

مثلاً:

Button

هم برای RTL و هم LTR همان Component است.

---

89. Motion and Component APIs

Motion باید Configuration/Token-based باشد، نه Hard-coded در هر Component.

---

90. Localization Safety

Theme، Direction و Motion نباید به زبان وابسته باشند.

برای:

fa
en

و Future Locales یک Architecture مشترک وجود دارد.

---

91. Theme Persistence Safety

تغییر Theme نباید Draft یا Local Data را Reset کند.

---

92. Direction Switching Safety

تغییر Locale/Direction نباید:

Draft
Selection
Context
State

را بدون دلیل از بین ببرد.

---

93. Direction Change Runtime

در صورت تغییر Runtime Direction، Layout باید:

Reflow

شود، نه اینکه Product Context از ابتدا ساخته شود.

---

94. Motion State Synchronization

Motion State باید با Application State هماهنگ باشد.

مثال:

Uploading

واقعاً باید در زمانی باشد که Uploading اتفاق می‌افتد.

---

95. Interruptible Motion

Motionهای غیرضروری باید قابل Interrupt باشند.

User نباید مجبور شود Animation را تماشا کند تا بتواند ادامه دهد.

---

96. No Blocking Animation

هیچ Animation غیرضروری نباید Actionهای عملیاتی را Block کند.

---

97. Operational Priority

برای Field Workflow:

Action
>
Feedback
>
Motion

است.

Motion هیچ‌وقت نباید بر Action غلبه کند.

---

98. GIS Priority

برای GIS:

Spatial Readability
>
Visual Novelty

است.

Motion نباید Feature مهم را مخفی کند.

---

99. Accessibility Priority

در کل Design System:

Meaning
>
Accessibility
>
Usability
>
Aesthetic Motion

اولویت دارد.

---

100. P4.7 Validation Matrix

Area| Status
Light Theme Contract| ESTABLISHED
Dark Theme Contract| ESTABLISHED
Theme Switching| ESTABLISHED
Theme Persistence| CONTRACT ESTABLISHED
RTL| ESTABLISHED
LTR| ESTABLISHED
Logical Layout Rules| ESTABLISHED
Mixed-direction Content| ESTABLISHED
Directional Icons| ESTABLISHED
Map Direction Rules| ESTABLISHED
Motion Principles| ESTABLISHED
Motion Categories| ESTABLISHED
Motion Durations| ESTABLISHED
Easing Strategy| ESTABLISHED
Reduced Motion| ESTABLISHED
Accessibility Motion Rules| ESTABLISHED
Cross-platform Theme| ESTABLISHED
Cross-platform Direction| ESTABLISHED
Cross-platform Motion| ESTABLISHED
Runtime Implementation| NEXT
Real Device Validation| NEXT
Accessibility Audit| NEXT
GIS Visual Validation| NEXT

---

101. P4.7 Non-Goals

P4.7 شامل موارد زیر نیست:

Flutter Implementation
React Implementation
Next.js Implementation
Production Theme Package
Production Animation Library
Map Engine Selection
Final Font Selection
Accessibility Certification
Runtime RTL Test
Runtime Dark Mode Test
Runtime Motion Test
Backend Change
Database Change
Migration

---

102. P4.7 Deliverables

این Gate موارد زیر را تثبیت می‌کند:

1. Dark Theme Contract
2. Theme Switching Rules
3. Theme Persistence Contract
4. RTL Contract
5. LTR Contract
6. Logical Layout Rules
7. Mixed-direction Rules
8. Directional Icon Rules
9. Map Direction Rules
10. Motion Principles
11. Motion Categories
12. Motion Duration Tokens
13. Easing Rules
14. Reduced Motion Contract
15. Accessibility Motion Rules
16. Theme / Direction / Motion Cross-platform Contract
17. GIS-specific Theme / Motion Constraints

---

103. P4.7 Gate Decision

Decision: PASS

Reason

P4.7 Design System را از نظر:

Theme
+
Direction
+
Motion

تکمیل می‌کند.

اکنون Light و Dark، RTL و LTR و Motion/Reduced Motion دارای یک Architecture مشترک و Semantic هستند.

---

104. P4.7 Critical Invariants

Dark Mode
≠
Color Inversion

RTL
≠
Separate Product

Motion
≠
Decoration Only

Reduced Motion
≠
Loss of Information

Theme Switch
≠
Context Reset

Direction Switch
≠
Data Reset

Map Direction
≠
UI Direction Mirroring

---

105. Handoff to P4 Consolidation

P4.7 موارد زیر را تحویل می‌دهد:

Color
Typography
Spacing
Layout
Components
Navigation
Map Controls
State
Feedback
Offline
Sync
Verification
Dark Mode
RTL/LTR
Motion

بنابراین کل Design System آماده بررسی نهایی P4 Consolidation است.

---

106. P4 End-State

در پایان P4 باید بتوان گفت:

Visual Language       = ESTABLISHED
Semantic Tokens       = ESTABLISHED
Typography            = ESTABLISHED
Spacing / Layout      = ESTABLISHED
Core Components       = ESTABLISHED
Navigation            = ESTABLISHED
Map Interaction       = ESTABLISHED
Operational States    = ESTABLISHED
Dark Mode             = ESTABLISHED
RTL / LTR             = ESTABLISHED
Motion                = ESTABLISHED
Reduced Motion        = ESTABLISHED

---

107. Remaining Validation Before Implementation

موارد زیر هنوز Runtime Validation می‌خواهند:

Real Device
Real Browser
Real Persian Font
Real English Font
Accessibility
Contrast
Dark Mode
RTL
Mixed Direction
GIS Overlay
Map Controls
Motion
Reduced Motion
Long Text
Localization

---

108. Design System Freeze Policy

حتی پس از P4.7:

Baseline
=
Established

اما:

Permanent Freeze
=
Only after Validation

است.

هر تغییر بعدی باید:

Evidence-based
Intentional
Documented
Backward-aware

باشد.

---

109. Final P4 Architecture

P4.1
Foundation
        ↓
P4.2
Color + Semantic
        ↓
P4.3
Typography + Spacing + Layout
        ↓
P4.4
Core Components
        ↓
P4.5
Navigation + Map Controls
        ↓
P4.6
State + Feedback + Recovery
        ↓
P4.7
Dark + RTL/LTR + Motion
        ↓
P4 Consolidation

---

110. Final Status

P4.7 = CLOSED / PASS

Dark Mode              = ESTABLISHED
Theme Architecture     = ESTABLISHED
RTL                    = ESTABLISHED
LTR                    = ESTABLISHED
Direction Rules        = ESTABLISHED
Motion System          = ESTABLISHED
Reduced Motion         = ESTABLISHED
Accessibility Rules    = ESTABLISHED
Cross-platform Model   = ESTABLISHED

Runtime Validation     = NEXT
P4 Consolidation       = NEXT
Technical Foundation   = AFTER P4 CONSOLIDATION

---

111. Final Principle

GeoField Design System اکنون باید در چهار سطح یک زبان مشترک داشته باشد:

Visual
Semantic
Operational
Spatial

و این زبان باید در:

Web
Mobile
Light
Dark
RTL
LTR
Online
Offline

تا حد امکان پایدار باقی بماند.

هدف نهایی:

Less Cognitive Load
+
More Operational Clarity
+
Reliable Spatial Interaction
+
Consistent Cross-platform Experience

است.

---

P4.7 CLOSED / PASS