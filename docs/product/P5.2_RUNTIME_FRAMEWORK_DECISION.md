GeoField — P5.2 Runtime & Framework Decision / Verification Gate

Project: GeoField
Repository: "VahidAfshin89/geofield-frontend"
Track: Frontend / Engineering / Architecture
Phase: P5 — Technical Foundation
Gate: P5.2
Status: COMPLETED / PASS — Decision Baseline Established
Branch: "main"

---

1. Gate Objective

P5.2 اولین Gate تصمیم‌گیری فنی Runtime در GeoField است.

هدف:

Evaluate
    ↓
Select
    ↓
Document
    ↓
Prepare Installation / Verification

برای:

Mobile Runtime
Web Runtime
Language
Framework
Build Tooling
Package Management

است.

---

2. Previous Gate

P5.1
Technical Foundation Strategy

Status:

CLOSED / PASS

P5.1 مشخص کرد که:

Architecture Before Installation
Evidence Before Installation

باید رعایت شود.

---

3. P5.2 Scope

P5.2 موارد زیر را تصمیم‌گیری می‌کند:

Flutter
Dart
React
Next.js
TypeScript
Node.js
Package Manager
Basic Runtime Tooling

اما هنوز شامل:

State Library
Map Engine
Local Database
Production CI/CD

نیست.

---

4. Decision Principles

تصمیم‌ها بر اساس:

Current Official Support
Compatibility
Product Requirements
Maintainability
Ecosystem
Cross-platform Fit
Offline Requirements
GIS Requirements
Reproducibility

انجام شده‌اند.

---

5. Mobile Runtime Decision

Selected

Flutter

Reason:

Cross-platform
Mobile-first capability
Mature UI system
Strong Android support
Single application codebase
Good fit for field-oriented UI

Flutter برای پروژه‌ای که Mobile آن Field-first و Offline-first است، Baseline مناسبی ایجاد می‌کند.

---

6. Flutter Version Baseline

مستندات رسمی فعلی Flutter در حال حاضر روی:

Flutter 3.44.7

هستند.

بنابراین:

Flutter Baseline
= 3.44.7 documentation baseline

است.

این به معنی تحمیل نصب دقیق همین Patch Version در تمام محیط‌ها نیست؛ نسخه واقعی SDK روی سیستم باید با:

flutter --version
flutter doctor

اثبات شود.

---

7. Dart Decision

Dart همراه Flutter مصرف می‌شود.

Dart 3.13 در 12 August 2026 منتشر شده است.

بنابراین:

Dart
→ Flutter-bundled SDK

به‌عنوان Strategy انتخاب می‌شود.

Dart مستقل نباید جداگانه برای پروژه نصب شود مگر نیاز مشخصی وجود داشته باشد.

---

8. Flutter Installation Principle

Flutter باید از Stable Channel استفاده کند.

Official documentation، Stable channel را مسیر مناسب برای استفاده Production معرفی می‌کند و ابزارهای "flutter" و "dart" از SDK در دسترس هستند.

---

9. Flutter Verification

حداقل Verification:

flutter --version
flutter doctor -v
dart --version

و:

flutter devices

---

10. Web Runtime Decision

Selected

React
+
Next.js
+
TypeScript

---

11. React Version Baseline

مستندات رسمی React در حال حاضر:

Latest Version = 19.2

را نشان می‌دهند.

Decision:

React Major
= 19

و Baseline:

React 19.2

است.

---

12. Next.js Decision

Next.js:

Selected

Reason:

React Framework
Routing
Rendering
Production Tooling
App Router
Integrated Build System

Next.js 16 در 21 October 2025 Stable منتشر شد و شاخه 16.x در حال حاضر Active LTS است.

Decision:

Next.js
= 16.x

---

13. Next.js Runtime Requirements

Next.js 16 نیاز دارد:

Node.js >= 20.9
TypeScript >= 5.1

و Node.js 18 دیگر پشتیبانی نمی‌شود.

---

14. Node.js Decision

Selected

Node.js 24 LTS

Node.js رسمی در حال حاضر:

v24.x = LTS

است و صفحه رسمی Download در حال حاضر "v24.19.0 LTS" را نشان می‌دهد.

---

15. Why Node 24 LTS

Node 24 به‌جای Current بودن، در خط LTS قرار دارد.

اصل:

Production Foundation
→
Active LTS / Maintenance LTS

در سیاست رسمی Node توصیه شده است.

بنابراین Node 24 نسبت به استفاده مستقیم از Node 26 Current برای Baseline پروژه محافظه‌کارانه‌تر است.

---

16. Node Version Baseline

Target:

Node.js 24.x LTS

برای reproducibility، نسخه واقعی نصب‌شده در محیط توسعه باید بعداً در:

package-manager / toolchain documentation

ثبت شود.

---

17. TypeScript Decision

Selected

TypeScript

Reason:

Static Typing
API Contract Safety
Refactor Safety
React Ecosystem
Maintainability

---

18. TypeScript Version Baseline

مستندات رسمی فعلی TypeScript 5.9 را مستند کرده‌اند.

Decision:

TypeScript
= 5.9.x baseline

---

19. Package Manager Decision

Selected

npm

Reason:

Bundled with Node.js
Low additional tooling
Beginner-friendly
Widely supported
Reproducible via package-lock.json

در این مرحله:

pnpm
yarn
bun

به‌عنوان Dependency اضافه نمی‌شوند.

این بدان معنا نیست که آن‌ها بد هستند؛ فقط برای Foundation اولیه، npm کم‌پیچیدگی‌تر است.

---

20. Web Application Bootstrap Strategy

Web باید بعداً با:

Next.js
App Router
TypeScript

Bootstrap شود.

Next.js رسمی برای ایجاد App جدید از "create-next-app" استفاده می‌کند و Defaultهای فعلی TypeScript و App Router را پشتیبانی می‌کنند.

---

21. Turbopack Decision

Next.js 16، Turbopack را برای "next dev" و "next build" به‌صورت Default Stable استفاده می‌کند.

Decision:

Turbopack
= ACCEPT AS DEFAULT

مگر اینکه در Runtime Validation مشکل مشخصی ایجاد شود.

---

22. Browser Baseline

Next.js 16 مستندات فعلی Browser baseline را:

Chrome 111+
Edge 111+
Firefox 111+
Safari 16.4+

ذکر می‌کند.

این مورد باید هنگام Web Validation در نظر گرفته شود.

---

23. Mobile IDE Strategy

Flutter با:

VS Code
Android Studio / IntelliJ

قابل استفاده است.

Official Flutter documentation همچنین مسیر Setup با VS Code را ارائه می‌کند.

IDE هنوز به‌عنوان بخش Core Runtime Freeze نشده است.

---

24. Android Priority

با توجه به Product Direction:

Android
= Primary Mobile Validation Target

است.

اما:

iOS
= Architecture Target

باقی می‌ماند.

---

25. Windows Development

محیط توسعه محلی فعلی پروژه Windows است.

Flutter مستندات رسمی برای نصب روی Windows را ارائه می‌کند و Git for Windows را نیز در Prerequisites قرار داده است.

---

26. Verification Before Installation

قبل از هر Installation جدید، ابتدا:

Check Existing Environment

انجام می‌شود.

نباید فرض کنیم:

Flutter Not Installed
Node Not Installed
npm Not Installed

بلکه باید ابتدا وضعیت واقعی سیستم بررسی شود.

---

27. Initial Environment Verification Commands

node --version
npm --version
npx --version

و:

flutter --version
dart --version
flutter doctor -v

---

28. Node Acceptance Criteria

Node باید:

Major = 24

و:

LTS

باشد.

---

29. npm Acceptance Criteria

npm باید:

Executable
Compatible with Node 24

باشد.

نسخه دقیق npm بعد از Verification ثبت می‌شود.

---

30. Flutter Acceptance Criteria

Flutter باید:

Stable
Executable
Doctor-visible

باشد.

---

31. Dart Acceptance Criteria

Dart باید:

Bundled with Flutter
Executable
Compatible with selected Flutter SDK

باشد.

---

32. React Acceptance Criteria

React در Application Manifest باید:

19.x

و Target:

19.2 baseline

باشد.

---

33. Next.js Acceptance Criteria

Next.js:

16.x

و باید با:

Node 24 LTS
React 19
TypeScript 5.x

سازگار باشد.

---

34. TypeScript Acceptance Criteria

TypeScript:

5.9.x baseline

است.

Exact patch بعد از package resolution در "package-lock.json" ثبت خواهد شد.

---

35. Version Pinning Policy

Baseline Version با Installed Version یکی نیست.

مراحل:

Official Baseline
        ↓
Installation
        ↓
Resolved Version
        ↓
Lockfile
        ↓
Reproducible Environment

---

36. Why Exact Patch Is Not Hard-coded in P5.2

چون Patch Version ممکن است بین زمان Decision و Runtime Installation تغییر کند.

بنابراین:

Major / Minor Policy
+
Lockfile

مرجع reproducibility خواهد بود.

---

37. Package Lock

npm باید:

package-lock.json

تولید و Commit کند.

---

38. Flutter Lockfile

Flutter باید:

pubspec.lock

را در Application قرار دهد، مطابق استراتژی رسمی پروژه و نوع Package.

---

39. No Global Package Explosion

هیچ پکیج UI یا Library جانبی بدون Gate مربوطه به‌صورت Global نصب نشود.

---

40. Runtime vs Library

P5.2 فقط Runtime/Framework Foundation است.

Libraries مانند:

State Management
Networking Library
Local Database
Map SDK
DI
Form Package

در Gateهای بعدی ارزیابی خواهند شد.

---

41. Map Engine Status

NOT FROZEN

Map Engine در P5.10 ارزیابی می‌شود.

---

42. State Management Status

NOT FROZEN

تصمیم در P5.6.

---

43. Local Storage Status

NOT FROZEN

انتخاب در Gate تخصصی Offline/Storage.

---

44. API Client Status

NOT FROZEN

در P5.7.

---

45. Testing Framework Status

NOT FROZEN

در P5.8.

---

46. Accessibility Tooling Status

NOT FROZEN

در P5.9.

---

47. Animation Tool Status

Rive:

OPTIONAL

است و در این Gate Dependency محسوب نمی‌شود.

---

48. 3D Tool Status

Three.js / React Three Fiber:

OPTIONAL

و در P5.2 نصب نمی‌شوند.

---

49. Design Tool Status

Figma:

Design Tool

است و Runtime Dependency نیست.

---

50. Technical Stack Baseline

Current baseline:

MOBILE
Flutter
Dart bundled with Flutter

WEB
React 19.2 baseline
Next.js 16.x
TypeScript 5.9.x
Node.js 24.x LTS

PACKAGE MANAGER
npm

---

51. Decision Summary

Technology| Decision| Status
Flutter| Selected| BASELINE
Dart| Flutter-bundled| BASELINE
React| 19.2 baseline| BASELINE
Next.js| 16.x| BASELINE
TypeScript| 5.9.x| BASELINE
Node.js| 24.x LTS| BASELINE
npm| Selected| BASELINE
Rive| Optional| DEFER
Three.js| Optional| DEFER
React Three Fiber| Optional| DEFER
Map Engine| Not selected| P5.10
State Management| Not selected| P5.6
Local DB| Not selected| Future Gate
API Client| Not selected| P5.7
Testing Stack| Not selected| P5.8

---

52. Compatibility Summary

Expected target:

Node 24 LTS
+
Next 16.x
+
React 19.x
+
TypeScript 5.x

Next.js 16 requires Node 20.9+ and TypeScript 5.1+, therefore Node 24 and TypeScript 5.9 exceed the documented minimum requirements.

---

53. Stability Strategy

برای Foundation:

LTS Runtime
+
Stable Framework
+
Pinned Lockfile

ترجیح داده می‌شود.

---

54. Reversibility

Decisions باید قابل بازبینی باشند.

مثلاً:

npm

بعداً در صورت Requirement واقعی می‌تواند به Package Manager دیگری تغییر کند، اما نباید بدون دلیل در میانه Foundation تغییر داده شود.

---

55. Beginner Safety

چون Technical Foundation نباید پیچیده‌تر از نیاز باشد:

Install Only What Is Needed

اصل رسمی است.

---

56. Installation Sequence

ترتیب:

1. Check Node
2. Check npm
3. Check Flutter
4. Check Dart
5. Install only missing Runtime
6. Verify
7. Bootstrap project
8. Verify project
9. Commit

---

57. No Blind Upgrade

اگر سیستم قبلاً Version قابل‌قبولی دارد:

DO NOT UPGRADE IMMEDIATELY

ابتدا Compatibility را بررسی می‌کنیم.

---

58. Flutter Upgrade Rule

Flutter documentation برای Upgrade کردن ابزارها دستور:

flutter upgrade

و برای Dependencyهای پروژه:

flutter pub upgrade

را مستند کرده است.

اما P5.2 نباید Upgrade را بدون بررسی وضعیت فعلی سیستم انجام دهد.

---

59. Node Upgrade Rule

Node باید فقط وقتی تغییر کند که:

Current Version
<
Required Baseline

یا:

Compatibility Problem

وجود داشته باشد.

---

60. Runtime Verification

Runtime Verification باید شامل:

Version
Path
Executable
Toolchain Health

باشد.

---

61. Final P5.2 Validation Matrix

Area| Status
Flutter Selected| PASS
Dart Strategy| PASS
React Selected| PASS
Next.js Selected| PASS
TypeScript Selected| PASS
Node LTS Selected| PASS
npm Selected| PASS
Compatibility Assessment| PASS
Version Governance| PASS
Installation Policy| PASS
Verification Policy| PASS
Current Local Installation| PENDING LOCAL VERIFICATION
Bootstrap Application| NEXT
State Library| NEXT
Map Engine| NEXT
Storage| NEXT
API Client| NEXT

---

62. P5.2 Important Distinction

این Gate ثابت می‌کند:

Selected Stack

ولی هنوز ادعا نمی‌کند:

Local Machine Successfully Configured

تا زمانی که Verification Commands واقعاً روی دستگاه اجرا نشده باشند.

---

63. P5.2 Non-Goals

این Gate شامل:

Production UI
Component Implementation
Map Engine
State Management
Local Database
Offline Engine
API Client
CI/CD
Deployment

نیست.

---

64. Gate Decision

Decision: PASS — TECHNICAL BASELINE SELECTED

Rationale

Stack انتخاب‌شده با شرایط فعلی اکوسیستم و Requirements پروژه سازگار است:

Flutter
+
React
+
Next.js
+
TypeScript
+
Node LTS
+
npm

و برای هر Runtime مسیر Verification مشخص شده است.

---

65. P5.2 Exit Criteria

Runtime Direction         ✓
Framework Direction       ✓
Version Baseline          ✓
Compatibility             ✓
Package Manager            ✓
Installation Policy        ✓
Verification Policy       ✓
Reproducibility Strategy  ✓

Local Installation Proof  →
Project Bootstrap         →

---

66. Handoff to P5.3

P5.3 باید روی:

Repository Architecture
Package Architecture
Web Structure
Mobile Structure
Shared Contracts
Platform Boundaries
Feature Boundaries
Testing Boundaries

تمرکز کند.

---

67. P5.3 Must Preserve P4

P5.3 نباید:

Design System
Navigation
Map Semantics
State Semantics

را دوباره طراحی کند.

---

68. Final Technical Baseline

MOBILE
Flutter Stable
Dart via Flutter SDK

WEB
React 19.2 baseline
Next.js 16.x
TypeScript 5.9.x

RUNTIME
Node.js 24.x LTS

PACKAGE MANAGEMENT
npm

MAP
DEFERRED

STATE MANAGEMENT
DEFERRED

LOCAL STORAGE
DEFERRED

API CLIENT
DEFERRED

TESTING
DEFERRED

---

69. Final Status

P5.2 = CLOSED / PASS
        ↓
Runtime Baseline Selected
        ↓
Local Environment Verification = NEXT
        ↓
P5.3 = NEXT

---

P5.2 RUNTIME & FRAMEWORK DECISION / VERIFICATION GATE — CLOSED / PASS